// Single flip to fully disable the Teacher Prep surface without deleting code.
// When false, /teacher falls back to the Companion app like every other path.
export const TEACHER_PREP_ENABLED = true;
