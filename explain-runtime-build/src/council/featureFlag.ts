// Single flip to fully disable the Council surface without deleting code —
// same idiom as teacherprep/featureFlag.ts and durin/featureFlag.ts. When
// false, /council falls back to the Companion app like every other path.
export const COUNCIL_ENABLED = true;
