# Founder Handoff — 2026-07-12 v1.1 Amendment (Commands A–E)

Repository: `KWilkinsonSales/explain-runtime-wedge` · Base `main` @ `c532d9f`

## Work delivered

| Product | Branch | PR | Head SHA | Receipt |
| --- | --- | --- | --- | --- |
| LDS Teacher Preparation v1.1 | `claude/lds-teacher-prep-v1-1` | #23 | `3ceca14` (+ this handoff commit) | `proof/teacherprep/RECEIPT-v1.1.md` |
| Companion runtime v1.2 | `claude/companion-runtime-v1-2` | #24 | `960ebf2` | `proof/companion/RECEIPT.md` |

Supporting records: `proof/RECON-2026-07-12.md` (Command A),
`proof/DEPLOYMENT-2026-07-12.md` (Command D). Products remain fully
separate: distinct entry routes, state namespaces, copy, and receipts;
shared repository/components only.

## Production URLs

- LDS Teacher Preparation: **https://companion-prototype-erw.netlify.app/teacher**
- Companion: **https://companion-prototype-erw.netlify.app/companion/prototype**
- Netlify project `companion-prototype-erw` · site `0fca5864-c706-435c-a178-30657b31f5f9`
  · production deploy at verification `6a53ca2ca5eeeb000836be6f` (main @ `c532d9f`)
- Pre-merge live review: `deploy-preview-23--…netlify.app/teacher` (LDS v1.1,
  where the current-week function runs live) and
  `deploy-preview-24--…netlify.app/companion/prototype` (Companion v1.2)

## Proof index

- **Tests**: LDS branch **129/129** (13 files); Companion branch **100/100**
  (11 files); typecheck and production build clean on both.
- **Privacy proof**: journal/notes exist only under `teacherprep.private.v1`
  (unit + in-browser localStorage assertions); zero non-localhost network
  requests on the LDS surface; exports exclude private material by default
  with explicit Teacher Packet opt-in only; no analytics or telemetry.
- **Duplicate-event proof (Companion)**: `companionResponseEngine` suite +
  in-browser — one admitted event → one execution → one rendered answer;
  duplicates suppressed; stale in-flight superseded; idempotent execute;
  provider-failure fallback sentence.
- **Source provenance proof (current-week CFM)**: server function fetches
  only the official public endpoint; strict double validation
  (server + client); labeled fixture fallback
  (“Illustrative — official current lesson could not be verified.”)
  proven in-browser; public metadata cached only while its date range covers
  today; nothing inferred from memory.
- **Print/export proof**: three presets + Print now from the frozen snapshot;
  private excluded by default (unit tests per preset); `@page` pagination;
  disclaimer retained.
- **Church Account feasibility**: no official supported developer API or
  Church Account OAuth path exists; no sign-in implemented; public official
  links are the production path (sources in the recon receipt).
- **Screenshots**: `proof/teacherprep/01–11` (phone + tablet, all LDS
  states), `proof/companion/01–04` (off/listening/text-mode/tablet).

## Known limitations (disclosed, not silently completed)

- CFM live parse unverified from the build environment (network policy);
  first live run may fall back to the labeled fixture until the extractor is
  tuned against the real payload — verifiable immediately on deploy-preview-23.
- Companion answers come from the deterministic governed rail; a real model
  provider is a separate authorization on the existing provider seam.
- PDF via the browser print dialog; per-browser localStorage persistence; no
  service worker; mobile-Safari check requires the founder's device.
- LDS hostname branding (companion-prototype-erw) — founder-side Netlify
  decision (rename / custom domain / second project).

## Exact instructions

Local run:

```bash
cd explain-runtime-build && npm install
npm run dev            # /teacher and /companion/prototype
npm run test && npm run typecheck && npm run build
node proof/teacherprep/browser-walkthrough.mjs
node proof/companion/browser-walkthrough.mjs
```

Rollback: see `proof/DEPLOYMENT-2026-07-12.md` (Netlify publish-previous,
git revert of the merge, or per-product feature flags).

## Final classifications

- **Companion: READY FOR FOUNDER LIVE RESPONSE-LOOP REVIEW**
- **LDS Teacher Preparation: READY FOR FOUNDER v1.1 PRODUCT REVIEW**

No Prototype Pass is declared; classification (Prototype Pass / Defect /
Amendment) belongs to the founder alone.

## Open production-proof gates

1. PRs #23 and #24 are unmerged — production still serves v1/v1.1-front-door
   code until the founder merges (deploy previews carry the new work now).
2. Live iPhone Safari verification of both direct links — founder device.
3. Current-week CFM live validation — observable on deploy-preview-23
   `/teacher` (validated official week, or the labeled fallback if the
   extractor needs tuning).
